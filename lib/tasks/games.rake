namespace :games do
  task :gametwo => :environment do
    # game2 = Game.create!(name:"Game 2: A little bit louder, now...", description: "welcome to zombocom", start_date: (Time.now+1.days).start_of_day, end_date: (Time.now+1.days).end_of_day)

    #ListItem.create!(name: "the end of the rainbow", game_id: game2.id)
    #ListItem.create!(name: "volun-told as tribute", game_id: game2.id)
    #ListItem.create!(name: "something you'd rather not see ever again", game_id: game2.id)
    #ListItem.create!(name: "salt of the earth", game_id: game2.id)

    #ListItem.create!(name: "it's the end of the world as we know it", game_id: game2.id)
    #ListItem.create!(name: "something out of place", game_id: game2.id)
    #ListItem.create!(name: "good to the last drop", game_id: game2.id)
    #ListItem.create!(name: "love, love, love is everywhere", game_id: game2.id)
  end
end