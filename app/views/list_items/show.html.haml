.entry
  %h1
    = @list_item.name
  - if @list_item.flavor 
    %h2
      = @list_item.flavor
  .content
    - if @entry
      = image_tag @entry.image
    = render partial: "form", locals: {list_item: @list_item, entry: @entry}